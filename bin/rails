#!/usr/bin/env ruby
# frozen_string_literal: true

# This command will automatically be run when you run "rails" from the root of your application.

ENGINE_ROOT = File.expand_path('..', __dir__)
ENGINE_PATH = File.expand_path('../lib/hello/engine', __dir__)

require 'rails/all'
require 'rails/engine/commands'
